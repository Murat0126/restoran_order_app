name: restaurant_server
description: Локальный POS-сервер ресторана (Dart Frog + SQLite + WebSocket).
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.5.0

dependencies:
  bcrypt: ^1.1.3
  collection: ^1.18.0
  dart_frog: ^1.1.0
  dart_frog_web_socket: ^1.0.3
  image: ^4.5.4
  qr: ^3.0.2
  shared_models:
    path: ../packages/shared_models
  sqlite3: ^2.4.0
  uuid: ^4.5.1
  web_socket_channel: ^3.0.1

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0

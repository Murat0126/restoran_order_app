.dart_tool/
.dart_frog/
.packages
.pub/
build/
pubspec.lock
.data/

# Собранная Flutter Web (копируется build_and_run.ps1).
# Сами исходники лежат в client_menu/, в репо не дублируем.
public/*
!public/.gitkeep

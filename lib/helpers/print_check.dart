// Примерная функция для печати чека
import '../data/models/order.dart';

Future<void> printReceipt(Order order) async {
  // final printer = BluetoothPrinter(); // Здесь ваш экземпляр принтера
  //
  // // Печать данных заказа
  // printer.printText('Чек для стола: ${order.tableId}');
  // order.orderedDishes.forEach((dish) {
  //   printer.printText('${dish.name} - ${dish.discountPrice} сом');
  // });
  // printer.printText('Итого: ${order.orderedDishes.fold(0, (total, dish) => total + dish.discountPrice)} сом');
  // printer.printText(order.isPaid ? 'Оплачено' : 'Не оплачено');
}
